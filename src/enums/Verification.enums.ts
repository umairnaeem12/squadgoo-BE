export enum VerificationPurpose {
    SIGNUP = "signup",                // for email verification during registration
    FORGOT_PASSWORD = "forgot_password",  // for forgot password OTP
    PASSWORD = "password",            // for storing login/reset password hashes
}
